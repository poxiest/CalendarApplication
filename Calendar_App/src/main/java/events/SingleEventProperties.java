package events;

/**
 * An enum including available event properties.
 */
public enum SingleEventProperties {
  SUBJECT,
  STARTDATE,
  STARTTIME,
  ENDDATE,
  ENDTIME,
  ALLDAYEVENT,
  DESCRIPTION,
  LOCATION,
  PRIVATE;
}
